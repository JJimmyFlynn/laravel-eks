apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: laravel
  namespace: laravel
spec:
  ingressClassName: alb
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: laravel-service
                port:
                  name: http
